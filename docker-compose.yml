version: '3.7'

services:
    backend:
        build:
          context: ./API/simple-api-student-main
          dockerfile: Dockerfile
        networks:
          - app-network
        depends_on:
          - database

    database:
        build:
          dockerfile: Dockerfile
        networks:
          - app-network

    httpd:
        build:
          context: ./http
          dockerfile: Dockerfile
        ports:
          - "8085:80"
        networks:
          - app-network
        depends_on:
          - backend
          - database


networks:
    app-network:
