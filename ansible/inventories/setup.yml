all: 
  vars:
    ansible user: centos
    #ansible_ssh_private_key_file: ~/Desktop/EPF_4A/DevOps/ansible/id_rsa 
    ansible_ssh_private_key_base64: $ ${{ secrets.ID_RSA }}
  children:
    prod:
      hosts: leo.goutard.takima.cloud